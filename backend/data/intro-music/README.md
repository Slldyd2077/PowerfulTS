# 开屏背景音乐目录

把音频文件放进这个目录，登录页开屏会**随机播放**其中一首，左侧音频频谱会随音乐**真实律动**。

## 支持的格式

`.mp3` `.wav` `.ogg` `.m4a` `.flac` `.aac`

## 用法

1. 把任意数量的音频文件拖进本目录（子目录会被忽略）。
2. 启动后端，打开登录页即可。每次进入登录页会随机选一首；播放结束自动换下一首。

> 曲目显示名取文件名（去扩展名）。无需维护清单——后端会自动扫描本目录。
> 目录为空时，登录页频谱会回退为模拟律动，不会报错。
